

## Create BRIANNAWARREN Coupon in Stripe

### What I'll Do
- Create a **100% off** coupon in Stripe with the name/ID **BRIANNAWARREN**
- Set it as a **one-time use per redemption** ("once" duration)

### Redemption Limit Note
The coupon creation tool supports setting the discount and duration, but the **max redemptions (10 uses)** limit may need to be configured directly in your Stripe dashboard after creation. I'll create the coupon first, and then we can verify the limit is set.

### How It Will Work
Once created, your friend can enter **BRIANNAWARREN** as a promo code during checkout and get the song for free — up to 10 times.

