

## Remove "Includes full download access" from Bonus Unlock Section

### What changes

**`src/pages/SongPlayer.tsx`** (~line 1119–1121): Delete the `<p>` tag containing "Includes full download access". The bonus $19.99 unlock grants listening access only — downloading requires the separate $49.99 purchase, same as the primary song.

One line removal, no other files affected.

